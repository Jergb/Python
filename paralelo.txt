Equivalencias o.O

find(variable<valor)
np.nonzero(variable < valor)

variable(find(variable<valor))
variable[np.nonzero(variable < valor)]

variable(find(variable(variable<=valor_a)>valor_b))
variable[np.nonzero(variable <= valor_a)][np.nonzero(variable[np.nonzero(variable <= valor_a)] > valor_b)]

[inicio:incremento:final]
range(inicio,final,icremento)  incremento no puede ser float, crea una objeto tipo range
linspace(inicio,final,intervalos) crea un vector
arange(inicio,final,incremento) crea un vector

variable(posición)=[]
variable.remove(posición)
variable.remove(variable[elemento])

for i=inicio:incremento:final
for i in linspace(inicio, final, incremento):

mean(variable)
sp.mean(variable)